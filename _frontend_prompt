For entire frontend project, use elegant style and colors.
Make the data fields to show full data value that is not slipped.
Make data fields width automatically so that no overflow happen.
Match table header field color theme and its table body row field color theme.

My main colors are #0f0f0f, #089981, #f23645.
My secondary colors for extra informations are #f4e07a, #4c7ef0, #f69768.
Consider this colors for project color theme.













I need a frontend project built with React which interacts with current backend project.

This project is a meme coin trading bot project.

All navigations are in Header.

In tokens page, show tokens list in a table with pagination, when I click a token raw, a collape is opend under the token raw component and in the collapse body it'll show the detailed token informations with graph, don't show the graph right now.

In transactions table, it has a table that shows newly happening token transactions.

In the "Strategy" header dropdown, I have only TPSL right now, which shows the TPSL rules I added.

This TPSL page has add new TPSL rule button, which shows input fields to add a new rule.

For each rules in TPSL page, it has Simulation button, which shows the simulating results.

All detailed information of any data is shown in a collapse styled component.

Each data listing page has a section that has global search, seperated fields search on top of data table.

Table has sort functionality for each table header fields.

Make the project structure very well, for scalability, reusability, maintainability.
Especially for route design, Redux design, data flow design, component reusability.

Use TailwindCSS for styling. I want a clean, elegant, robust, and looks so great UI/UX.

Make this frontend project in "frontend" folder.