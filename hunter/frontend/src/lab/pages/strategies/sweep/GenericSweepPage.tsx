import { GenericSweepView } from './GenericSweepView';

/** The one generic-engine grouped sweep page (redesign FE5) — replaces the three
 *  per-strategy sweep pages (tpsl1 / tpsl2 / swing1). */
export function GenericSweepPage() {
  return <GenericSweepView />;
}
