# 3Xk2Eu (mid-tape one-shot): its logic, and where the method stands

One wallet, one case file (derive 13). Its event is spelled and reachable, and the money is in which coins it watches - which is exactly what is unread.

The method is the nine steps on the first page of [../_!___derive.md](../_!___derive.md).
Its **chain rows** - every step ever run on it, dead ends included - are the rows naming
`3Xk2Eu` in the node chain, [mid-tape-rule-3.md](mid-tape-rule-3.md) section 2; the inventory
reaches them as `mt3 <row>`. Tapes, seat and frame: the head of that file. New work on this
wallet writes its rows here.

---

## 0. Checklist - read this first, every session

| step | state | where |
| --- | --- | --- |
| 1 Pick | done | row `3-4`: RACE +8.35 on 6/6 days, 998 episodes, hold p50 50 s |
| 2 Portrait | **not done** | no portrait exists. The scan ran first, which is the order this method now refuses |
| 3 His exit | **not done** | row `exit read 3Xk2Eu` shows no static rule names more than 12.4 % of his sells and he sits through the crossing on ~73 %. His own exit was never read the 8.0 way |
| 4 Test each clause | done | rows `6.1 ladder 3Xk2Eu`, `5.2 on the spelled E`: flow_spike >= 7.56 AND a thin tape, cover 30.5 %, peak leftover +5.68 % |
| 5 Our version | partly | 5.2 ran without its random-print control; none of the four 5.4 rows is read |
| 6 Fit D, P, X | **made the wrong way** | P, X and D were all searched under a 55.5 s clock, not under his own exit. Under derive 8.0 those books are unread, not red |
| 7 Coverage | done | section 5 of this file |
| 8 Prove | no | no sentence has reached the gates |
| 9 Record | done | the chain rows, and section 2 of the workflow |

---

## 1. His logic (derive 3)

> **Not written.** The measurements say what he answers, never why he watches that coin.

Where: unread - step 2 is the next step.

---

## 2. His own exit (derive 8.0)

**Not read.** No static rule names more than 12.4 % of his sells, and he sits through the first crossing on about 73 % of positions, so his exit reads the state of the trade. Every P, X and D book on him was priced under a 55.5 s clock instead, which is why they count as unread.

Where: row `exit read 3Xk2Eu`.

---

## 3. His sentence

The laddered E and its books, [mid-tape-rule-3.md](mid-tape-rule-3.md) section 2 and the evidence ledger row **Mid-tape 3Xk2Eu**.

---

## 4. Coverage and the unread list (derive 10.1)

Every inventory family, marked **tried** / **partly** / **not tried** / **no data** /
**not his**. Two rules decide a mark, and both bite here: a fact read **alone** is not
tried (derive 6.1), and a book priced under a **placeholder exit** is not tried (derive
8.0). Where a mark is uncertain it reads **not tried**, which keeps the idea alive; the
expensive error is marking something tried that was never properly read.

| family | mark | what was run | what is left |
| --- | --- | --- | --- |
| D1 creation fingerprint | partly | the creation fingerprint reads AUC 0.46-0.51; the best cell of the study is a prior-only launcher history at -1.10 % on 4/6, and it is orthogonal to him | the same under his own exit rather than a clock |
| D2 creator's document (metadata URI) | not tried | the row reports **coverage only**: `cvx_meta` is the keep+ep50 sidecar, so a left-join makes the door a universe filter (law 30). Coverage alone is worth 8 points | the whole family, once a sidecar covers every coin on the tape. This is his largest untried door |
| D3 this coin's life before the fire | partly | every tape coin fact is red (best `buys10` AUC 0.626, every quintile -2.5..-7.3 %) and the coin's life shape is red (best -2.33 %) | the same under his own exit |
| D4 loss door | partly | the loss door lifts -6.52 to -3.92 % and stays 0/5 | the same under his own exit |
| D5 off-chain | no data | outside the data scope (derive 2.0) | - |
| E1 a listed ix structure acts | partly | a public-app build is a term of the tightened sentence | a build list of its own, read as E1 rather than as one term |
| E2 silence, then a spend | not tried | - | the whole family. `structure_burst` is in the 5.1 scan at lift 4.25 and was never taken as a root |
| E3 an operator's plan is unfinished | not tried | - | `clip_step_up` reads 4.64 in the 5.1 scan and is never read as a root |
| E4 a count crosses a line | not tried | - | the whole family |
| E5 after sellers | not tried | - | `buy_after_sells` reads 4.27 in the 5.1 scan and is never read as a root |
| E6 this print | tried | the 6.1 ladder spells E in both folds; `alone_in_slot`, its own top 5.1 tell, is 0.08 % acted and sits at depth 6 | - |
| E7 clock | not tried | - | the whole family |
| P1 curve position | partly | `room` under the wall is the only separator (AUC 0.636); its best money cut is -4.46 % on 0/6 | the same under his own exit |
| P2 windowed tape metrics | partly | `nb5` and `mv10` are ladder terms; the 24 mode x exit cells split on the 60 s move are all red | the same under his own exit |
| P3 skin in | not tried | - | the whole family |
| P4 ix makeup of the recent tape | partly | a public-app build is a term of the tightened sentence | seed racers, tools and the buy-SOL band |
| P5 tape state already true | partly | a thin tape (`nb5 <= 2`) is the second ladder term | the rest of the family |
| X1 static | tried | no static rule names more than 12.4 % of his sells, on both halves of the days | - |
| X2 the tape stops | not tried | - | the whole family. His exit is state-dependent and this is where to look first |
| X3 another actor acts | not tried | - | the whole family |
| R re-entry | not tried | - | one open at a time is assumed, never measured |
| S size | not tried | - | every book is at 0.2 SOL |
| 5.4 an earlier sign | not tried | - | `mv10` is the term that fires on the move already in the price; the state before it is unread |
| 5.4 a slower part of the same move | not tried | - | his hold p50 is 50 s, the longest of the node: a later, slower entry is plausible for him |
| 5.4 the other side | not tried | - | he avoids sells (0.15-1.39 in the 5.1 scan) |
| 5.4 another of his decisions | not tried | - | his re-entries and adds |

**Unread, in the order to run it:**

1. **Step 2, the portrait, on his coin choice.** The one green universe is his own coins **before he arrives**: +13.12 % a trade on 6/6 days, against -1.32 % after and -5.25 % elsewhere. Read 20 coins he came to against 20 where the same event fired and he did not, and write why.
2. **Step 3, his own exit.** It is state-dependent; the curved trail and the tape-stops family are the first candidates.
3. **D2, the URI family**, on a sidecar covering every coin. His largest untried door.
4. **Everything in step 6, again**, once his exit is read: P, X and D were all priced under a clock.

The next step itself is [_!___workflow.md](../_!___workflow.md) section 2, which points
here for the list.
