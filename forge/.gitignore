# Rust
/target
target-check
**/*.rs.bk

# Env / secrets
.env
.env.backup
*.pem
keystore/
*.enc
# package-lock.json is intentionally COMMITTED — the frontend image builds with
# `npm ci`, which requires an in-sync committed lockfile (see root .gitignore).


# Lake data (workstation-local Parquet; never committed)
/lake-data
*.parquet

# OS / editor
.DS_Store
Thumbs.db
desktop.ini

# Frontend
frontend-launch/node_modules
frontend-launch/dist
frontend-launch/tsconfig.tsbuildinfo
