//! Domain models (typed rows / DTOs) — Phase 3. One module per domain
//! (dimensions, tokens, feed, own-launch, strategy). Amounts exposed as `_*`
//! human values converted from `amount_*` base-unit columns at the repo boundary
//! via [`crate::units`]. Placeholder now.
