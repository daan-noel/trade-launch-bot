//! Config / settings — Phase 3. Loads `.env` (dotenvy), owns `Settings`
//! (database_url, pool sizes, ports) and the boot-time wiring. Placeholder now so
//! the crate map is visible from commit 1.
