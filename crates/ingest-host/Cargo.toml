[package]
name = "ingest-host"
version.workspace = true
edition.workspace = true
description = "Host adapter: bridges the borrowed `ingest-laserstream` raw transport onto platform-core's raw_txs/trades rows via the pump.fun/SOL venue adapter. LIVE ONLY — never a lab dep."

[lib]
name = "ingest_host"
path = "src/lib.rs"

[dependencies]
platform-core = { path = "../platform-core" }
# Borrowed standalone transport (no workspace deps of its own). LIVE-only.
ingest-laserstream.workspace = true
tokio.workspace = true
tracing.workspace = true
anyhow.workspace = true
sqlx.workspace = true
