[package]
name = "pump-constants"
version = "0.1.0"
edition = "2021"
description = "Single-source protocol addresses, unit conversions, and trader tuning constants. Zero dependencies."

[lib]
name = "pump_constants"
path = "src/lib.rs"

[dependencies]
